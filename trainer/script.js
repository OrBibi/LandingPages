document.addEventListener('DOMContentLoaded', () => {const form = document.getElementById('lead-form');const successMsg = document.getElementById('thank-you-msg');if (form) {form.addEventListener('submit', async (e) => {e.preventDefault();const btn = form.querySelector('button[type="submit"]');const originalText = btn.innerText;btn.innerText = 'מעבד...';btn.disabled = true;const formData = new FormData(form);const data = Object.fromEntries(formData.entries());try {if (window.submitToBackend) {await window.submitToBackend(data);} else {console.log('Simulating backend submission:', data);await new Promise(r => setTimeout(r, 1000));}form.style.display = 'none';if (successMsg) {successMsg.classList.remove('hidden');successMsg.style.display = 'block';}} catch (error) {console.error('Submission failed', error);btn.innerText = originalText;btn.disabled = false;alert('משהו השתבש. אנא נסה שוב.');}});}});console.log('Page Loaded');