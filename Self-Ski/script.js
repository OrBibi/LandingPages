
// --- UI/Animation Logic (Designer Agent) ---
console.log('Page Loaded');


// --- Backend/Google Forms Logic (Integrator Agent) ---
(function() {    const form = document.getElementById('lead-form');    const submitButton = document.getElementById('submit-btn');    const googleFormUrl = 'GOOGLE_FORM_URL';    const fieldMappings = {};    if (form && submitButton) {        form.addEventListener('submit', async function(event) {            event.preventDefault();            submitButton.textContent = 'Sending...';            submitButton.disabled = true;            const formData = {};            for (const htmlFieldName in fieldMappings) {                const entryId = fieldMappings[htmlFieldName];                const inputElement = form.querySelector(`[name="${htmlFieldName}"]`);                if (inputElement) {                    formData[entryId] = inputElement.value;                }            }            const params = new URLSearchParams(formData);            try {                await fetch(googleFormUrl, {                    method: 'POST',                    mode: 'no-cors',                    headers: {                        'Content-Type': 'application/x-www-form-urlencoded'                    },                    body: params                });                const formContainer = form.parentElement;                if (formContainer) {                    form.style.display = 'none';                    const successMessage = document.createElement('div');                    successMessage.className = 'text-green-500 text-lg font-semibold mt-4';                    successMessage.textContent = 'Thank you for your interest! We\'ll be in touch.';                    formContainer.appendChild(successMessage);                }            } catch (error) {                console.error('Form submission error:', error);                alert('There was an issue submitting your form. Please try again.');            } finally {                submitButton.textContent = 'Get Updates';                submitButton.disabled = false;            }        });    }})();